# =============================================================================
# Environment Configuration
# =============================================================================
# Copy this file to .env.local for local development
# Copy this file to .env.prod for production (and set real values)
#
# Usage:
#   Local:  cp .env.example .env.local && ln -sf .env.local .env
#   Prod:   Use .env.prod with docker-compose.prod.yml
# =============================================================================

# Database (PostgreSQL)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/knou_rate_course

# Redis (optional - leave empty to use in-memory cache)
# In-memory cache works but doesn't persist across restarts or share between instances
REDIS_URL=redis://localhost:6379
# For production without Redis, comment out the line above or set:
# REDIS_URL=

# Debug mode (true for development, false for production)
DEBUG=true

# JWT Configuration
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=local-dev-secret-for-local-and-testing-1234567890

# Frontend URL (for email verification links)
FRONTEND_URL=http://localhost:3000

# SendGrid Email (optional - leave empty to disable email sending)
SENDGRID_API_KEY=
FROM_EMAIL=no-reply@knouhoney.com

# Sentry Error Tracking (optional - leave empty to disable)
# Get your DSN from https://sentry.io
SENTRY_DSN=
